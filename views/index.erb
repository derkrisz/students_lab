<% @students.each do |student| %>
       <p><a href="/students/<%= student.id %>"><%= student.first_name %> <%= student.last_name %></p>
       <hr>
  <% end %>
  <a href="students/new">Add student</a>
